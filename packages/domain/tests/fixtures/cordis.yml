- name: '@fixture/agent-team-runtime'

- name: '@deepseek-ai/dsh-agent-team-ultra'
  config:
    defaultContinuationProvider: fixture-provider
    maxProfiles: 1
    maxProfileBytes: 4096
    maxHooks: 2
    maxAssignmentBytes: 2048
